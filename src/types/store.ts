export enum StoreSliceEnum {
    USERS = 'users',
    POSTS = 'posts',
    POST_COMMENTS = 'postComments'
}

export enum ThunkStatusEnum {
    PENDING = 'pending',
    FULFILLED = 'fulfilled',
    REJECTED = 'rejected'
}

